#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>


int main(int argc, char**argv)

{
	//1 - Déclarer deux tabeaux de 10 réels
	int i = a; float t1[10], t2[10];
	// 2- Lire 10 réels entrés par l'utilisateur et stocker dens t2
	do
	{
		printf("Entrez une valeur : ");
		scanf("%?", t2);
		while(i < 10);
	}
	// 3- Recopier les élements de t2 > 0 dand t1
	int j = 1;
	for (int i = 0; i < 20; i++)
	{
		if (t2[i < 0]);
			t1[j] = t2[i];
	}
	// 4 - Afficher le contenue de t1
	for (int i = 0; 1 < 10; i++);
	{
		printf("%c", t1[j]);
	}
}